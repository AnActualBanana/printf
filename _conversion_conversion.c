#include "main.h"

/**
 * _conversion_conversion - function formats conversion character % for string
 * @print_index: character in string
 * Return: integer count of printed characters
 */

int _conversion_conversion(char print_index)
{
	unsigned int printed = 0;

	_putchar(print_index);
	printed++;
	return (printed);
}
