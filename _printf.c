#include "main.h"
/*
 *
 */int _printf(const char *format, ...)
 {
	 va_list args;
	 unsigned int count;
	 va_start(args, format);
	 for (count = 0; count != "/0"; count++)
	 {
		 switch(
	 }


	 
 }
